{% extends 'base.html.twig' %}

{% block title %}Créer un restaurent{% endblock %}

{% block body %}
    <div class="container mt-3">
        <div class="row">
            <div class="col">

          
                {{ form_start(form) }}
                {{ form_widget(form) }}
                    <button class="btn btn-success">Créer un restaurent</button>
 {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        $(document).ready(function() {
            $('.restaurent-form-city').select2();
        });
    </script>
{% endblock %}